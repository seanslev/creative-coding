function setup() {
  // create a canvas
  createCanvas(1000, 1000);

  // disable animation
  noLoop();
}

function draw() {
  background(220);

  rectMode(CENTER);
 
}

function setup() {
  circle(50, 50, 25);

}